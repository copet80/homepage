export const DEBUG = false;

export const RENDER_CANVAS = 0;
export const RENDER_CSS = 1;

export const MODE_WANDER = 0;
export const MODE_FLOCK = 1;
export const MODE_GRID = 2;
export const MODES = [MODE_WANDER, MODE_FLOCK, MODE_GRID];

export const NUM_CHAR_COLS = 12;
export const NUM_CHAR_ROWS = 12;
export const NUM_CHARS = NUM_CHAR_COLS * NUM_CHAR_ROWS;
export const NUM_EXPLOSIONS = 20;
export const CHANGE_UP_INTERVAL = 5 * 1000;
export const CHANGE_MODE_INTERVAL = 15 * 1000;
